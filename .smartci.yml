stages:
  - build
  - test
  - security

build_backend:
  stage: build
  script:
    - cd backend && echo "Build step completed"

test_backend:
  stage: test
  script:
    - cd backend && npm install
    - cd backend && npm test

security_scan:
  stage: security
  script:
    - cd backend && npm audit --audit-level=high